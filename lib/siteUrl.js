/** Public marketing URL for this deployment (Indonesia site: techchainglobal.id). */
export const PUBLIC_SITE_URL =
  (typeof process !== 'undefined' && process.env.NEXT_PUBLIC_SITE_URL) ||
  'https://www.techchainglobal.id';
