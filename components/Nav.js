/** Canonical Nav lives in app/components — this file re-exports so imports like `../components/Nav` from `app/page.js` resolve correctly. */
import Nav from '../app/components/Nav.js';
export default Nav;
